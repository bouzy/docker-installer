**docker-installer** manages the installation and updates for docker, portainer and portainer agent

## Supported OS
- Debian/Ubuntu

## Installation
Run the setup file `setup.sh`
```bash
$ sudo sh setup.sh help
```